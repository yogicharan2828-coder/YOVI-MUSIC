version: '3.9'

services:
  app:
    build: .
    container_name: jiosaavn-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
